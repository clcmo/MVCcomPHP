<IfModule mod_rewrite.c>

    RewriteEngine On
    RewriteBase /
    
    # Redirecionar para public se não estiver nela
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>